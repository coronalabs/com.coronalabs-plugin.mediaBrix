Sources for the plugin `plugin.mediaBrix`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.mediaBrix" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
